# Environment variables — never commit secrets
.env
.env.*
!.env.example

# Uploaded user files — served at runtime, never committed
uploads/

# Go build output
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out

# Vendor (if not using vendoring)
# vendor/
